export const extractTypeDefinitionFromViewSql = ({}: { name: string; sql: string }) => {
  // 0. grab selection array from query definition
  // 0. grab the columns being selected (i.e, everything between "SELECT" and "FROM")
  // const everythingAfterSelectKeyword = sql.split('SELECT')
  // const columnsBeingSelected =
  // const strippedSql: string = strip(sql);
};
