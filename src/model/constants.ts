export enum DatabaseLanguage {
  MYSQL = 'mysql',
}
export enum DefinitionType {
  RESOURCE = 'resource',
  QUERY = 'query',
}
export enum DataType {
  STRING = 'string',
  NUMBER = 'number',
  DATE = 'Date',
  NULL = 'null',
}
