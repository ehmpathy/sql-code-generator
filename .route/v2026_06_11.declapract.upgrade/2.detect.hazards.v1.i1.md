# 2.detect.hazards result

## status

hazard analysis complete

## test hazards

### node v20.12.2 → v22.21.0

**what changed**: .nvmrc upgraded from node 20 to node 22

**why it may break**:
- node 22 may have different behavior for native modules
- some packages may not support node 22 yet
- ci will use node 22 which may expose incompatibilities

**how to verify**:
```sh
nvm use
npm run test
```

### jest.integration.env.ts now sources keyrack

**what changed**: integration test env now calls `keyrack.source({ env: 'test', owner: 'ehmpath' })`

**why it may break**:
- requires keyrack configuration
- requires `.agent/keyrack.yml` to be present
- may fail if credentials not available

**how to verify**:
```sh
rhx git.repo.test --what integration --mode apply
```

### jest.integration.env.ts exports aws sso credentials

**what changed**: env file now exports aws credentials from sso profile for aws sdk v2 compat

**why it may break**:
- requires aws sso login
- requires `declapract.use.yml` with `awsAccountId` to trigger behavior

**how to verify**: n/a (this repo doesn't appear to have `declapract.use.yml`)

### jest 29 → jest 30 (major version upgrade)

**what changed**: jest upgraded from 29.3.1 to 30.2.0

**why it may break**:
- jest 30 introduces breaking changes in snapshot format
- test runner api changes
- @jest/globals 30.2.0 added (may conflict with implicit jest globals)

**how to verify**:
```sh
rhx git.repo.test --what unit --mode apply
```

### ts-jest → @swc/jest transform

**what changed**: jest config now uses @swc/jest instead of ts-jest for transforms

**why it may break**:
- different transpilation behavior
- decorators/metadata may behave differently
- source maps may differ

**how to verify**:
```sh
rhx git.repo.test --what unit --mode apply
rhx git.repo.test --what integration --mode apply
```

### test-fns 1.4.0 → 1.15.8

**what changed**: major version bump of test-fns

**why it may break**:
- given/when/then api may have changed
- useBeforeAll/useThen may have different behavior

**how to verify**:
```sh
rhx git.repo.test --what unit --mode apply
```

### @jest/globals added

**what changed**: explicit @jest/globals 30.2.0 added

**why it may break**:
- tests with implicit jest globals may conflict
- expect/describe/it types may differ

**how to verify**:
```sh
rhx git.repo.test --what types
```

### test command env var changes

**what changed**: test commands now use `${THOROUGH:-}` != "true"` instead of `-z $THOROUGH`

**why it may break**:
- different evaluation semantics
- CI environment may not set vars the same way

**how to verify**:
```sh
THOROUGH=true npm run test:unit
```

## cicd hazards

### npm ci → pnpm install

**what changed**: install workflow now uses pnpm instead of npm

**why it may break**:
- requires pnpm-lock.yaml to be present and correct
- node_modules structure differs between npm and pnpm
- cache key now includes pnpm-lock.yaml hash

**how to verify**: push branch and monitor CI install job

### ubuntu-20.04 → ubuntu-24.04

**what changed**: all workflow jobs use ubuntu-24.04

**why it may break**:
- different system libraries
- different default tools/versions
- mysql/postgres client versions may differ

**how to verify**: push branch and monitor CI

### actions versions upgraded

**what changed**:
- actions/checkout@v3 → @v4
- actions/setup-node@v3 → @v4
- actions/cache/restore@v3 → @v4
- aws-actions/configure-aws-credentials@v1 → @v4

**why it may break**:
- cache key format may differ
- node setup behavior may differ
- aws oidc auth replaces access keys

**how to verify**: push branch and monitor CI

### new test sharding system

**what changed**:
- new `enshard` job for dynamic test sharding
- new `test-shards-integration` and `test-shards-acceptance` jobs
- can use `jest.integration.shards.jsonc` and `jest.acceptance.shards.jsonc` files

**why it may break**:
- shard patterns may not match test files
- note: absent config files have fallback (single dynamic shard)

**how to verify**: push branch and monitor CI (fallback to single shard is safe)

### keyrack firewall added

**what changed**: new `rhachet keyrack firewall` step in CI workflows

**why it may break**:
- requires keyrack.yml configuration
- secrets may not pass firewall validation

**how to verify**: check `.agent/keyrack.yml` extant and configured

### aws credentials now use oidc

**what changed**: aws auth switched from access keys to oidc role-to-assume

**why it may break**:
- requires `creds-aws-role-arn` input instead of secrets
- requires `id-token: write` permission

**how to verify**: verify workflow caller passes correct inputs

## path hazards

### .eslintrc.js deleted

**what changed**: eslint config deleted (replaced by biome)

**why it may break**:
- any tooling referencing .eslintrc.js will fail
- ide eslint plugins may show errors

**how to verify**:
```sh
npm run test:lint
```

### .prettierignore deleted

**what changed**: prettier ignore file deleted

**why it may break**:
- any code referencing prettier will fail

**how to verify**: n/a (biome replaces prettier)

### depcheckrc.yml → .depcheckrc.yml

**what changed**: depcheck config renamed with dot prefix

**why it may break**:
- old path reference in npm commands would fail
- package.json already updated to use new path

**how to verify**:
```sh
npm run test:lint:deps
```

### tsc-alias added to build

**what changed**: build now runs `tsc-alias -p ./tsconfig.build.json` after tsc

**why it may break**:
- path aliases must be resolvable
- requires tsconfig paths configuration

**how to verify**:
```sh
npm run build
```

## config hazards

### biome.jsonc replaces prettier + eslint

**what changed**: new biome config with lint and format rules

**why it may break**:
- different formatting rules
- different lint rules (may flag new errors)
- import organization changed

**how to verify**:
```sh
npm run test:format
npm run test:lint
```

### tsconfig extends changed

**what changed**:
- `@tsconfig/node-lts-strictest` → `[@tsconfig/strictest, @tsconfig/node20]`
- target changed from es6 to es2020
- module/moduleResolution changed to node16

**why it may break**:
- stricter type checking may surface errors
- node16 module resolution has different behavior
- es2020 target may affect output compatibility

**how to verify**:
```sh
rhx git.repo.test --what types
npm run build
```

### tsconfig paths added

**what changed**: added `@/*` and `@src/*` path aliases

**why it may break**:
- extant imports may need updating
- jest moduleNameMapper must match

**how to verify**:
```sh
rhx git.repo.test --what types
npm run build
```

### domain-objects 0.22.1 → 0.31.9

**what changed**: major version bump of domain-objects

**why it may break**:
- api changes in DomainEntity/DomainLiteral
- serialization format may differ

**how to verify**:
```sh
rhx git.repo.test --what unit --mode apply
```

### @ehmpathy/error-fns removed, helpful-errors added

**what changed**: error utility package replaced

**why it may break**:
- different error class names
- different api for error handling

**how to verify**:
```sh
rhx git.repo.test --what types
```

## summary

| category | count | severity |
|----------|-------|----------|
| test hazards | 8 | high |
| cicd hazards | 6 | high |
| path hazards | 4 | medium |
| config hazards | 6 | high |

## recommended verification order

1. `rhx git.repo.test --what types` - catch type errors first
2. `npm run build` - verify build succeeds with tsc-alias
3. `npm run test:format` - verify biome format
4. `npm run test:lint` - verify biome lint
5. `rhx git.repo.test --what unit --mode apply` - run unit tests
6. `rhx git.repo.test --what integration --mode apply` - run integration tests
7. push to CI and monitor workflow execution
