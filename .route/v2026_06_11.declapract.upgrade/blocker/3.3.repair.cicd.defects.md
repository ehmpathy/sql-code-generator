# blocker: 3.3.repair.cicd.defects

## what blocks you

commit permission not granted. the stone requires a push to CI/CD, but `git.commit.uses get` shows no quota set.

## what you tried

1. verified all local tests pass (types, format, lint, unit, integration, acceptance)
2. checked commit permission status via `rhx git.commit.uses get`
3. result: "no quota set"

## what you need

human must grant commit permission:

```sh
rhx git.commit.uses allow
```

once granted, i can proceed with:
```sh
printf 'fix(practs): bump to latest best\n\n- upgraded declapract packages\n- applied latest practices' | rhx git.commit.set -m @stdin --mode apply --push
rhx git.release --watch
```
